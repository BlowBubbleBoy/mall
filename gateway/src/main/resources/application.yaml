spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  config:
    import:
      - optional:nacos:gateway-myconfig.yaml?refreshEnabled=true
